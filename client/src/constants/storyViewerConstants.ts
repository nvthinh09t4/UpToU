/** Milliseconds the answer feedback panel remains visible. */
export const FEEDBACK_DISPLAY_MS = 2800;

/** Milliseconds the milestone achievement banner remains visible. */
export const MILESTONE_DISPLAY_MS = 2500;

/** Milliseconds the score delta chip animation lingers before clearing. */
export const SCORE_DELTA_DISPLAY_MS = 1500;

/** Points-per-type threshold at which a milestone banner is shown. */
export const SCORE_MILESTONE_INTERVAL = 10;
